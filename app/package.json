{
  "name": "stenoai",
  "version": "0.1.3",
  "description": "Mac meeting transcriber with Apple Speech, system audio capture, local Whisper fallback, and local LLM summaries",
  "main": "main.js",
  "homepage": "https://github.com/akashiitd/Mac_meeting_Transcriber_app",
  "repository": {
    "type": "git",
    "url": "https://github.com/akashiitd/Mac_meeting_Transcriber_app.git"
  },
  "scripts": {
    "build-helper": "../scripts/build_apple_speech_helper.sh",
    "start": "npm run build-helper && electron .",
    "build": "npm run build-helper && electron-builder",
    "build-mac": "npm run build-helper && electron-builder --mac",
    "build:intel": "npm run build-helper && electron-builder --mac --x64",
    "build:arm64": "npm run build-helper && electron-builder --mac --arm64",
    "dist": "npm run build-helper && electron-builder --publish=never",
    "pack": "npm run build-helper && electron-builder --dir",
    "release": "electron-builder --publish=always",
    "version:patch": "npm version patch && git push && git push --tags",
    "version:minor": "npm version minor && git push && git push --tags",
    "version:major": "npm version major && git push && git push --tags",
    "release:patch": "npm run version:patch && npm run build",
    "release:minor": "npm run version:minor && npm run build",
    "release:major": "npm run version:major && npm run build"
  },
  "keywords": [
    "audio",
    "transcription",
    "recording",
    "whisper",
    "ollama",
    "ai",
    "meetings"
  ],
  "author": "StenoAI Team",
  "license": "CC-BY-NC-4.0",
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.0.0"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "build": {
    "appId": "com.stenoai.recorder",
    "productName": "StenoAI",
    "directories": {
      "output": "dist",
      "buildResources": "build"
    },
    "files": [
      "**/*",
      "!dist/**/*",
      "!build/**/*",
      "!assets/**/*",
      "!.git/**/*"
    ],
    "extraResources": [
      {
        "from": "../src",
        "to": "src",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": "../simple_recorder.py",
        "to": "simple_recorder.py"
      },
      {
        "from": "../requirements.txt",
        "to": "requirements.txt"
      }
    ],
    "mac": {
      "icon": "build/icon.icns",
      "category": "public.app-category.productivity",
      "target": {
        "target": "dmg"
      },
      "entitlements": "build/entitlements.mac.plist",
      "entitlementsInherit": "build/entitlements.mac.plist",
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "identity": "Skrape Limited (HSDX294RG4)",
      "notarize": false,
      "extendInfo": {
        "NSMicrophoneUsageDescription": "StenoAI needs microphone access to record and transcribe meetings.",
        "NSAudioCaptureUsageDescription": "StenoAI needs system audio capture access to transcribe other speakers from meeting apps.",
        "NSSpeechRecognitionUsageDescription": "StenoAI uses Apple Speech recognition to transcribe meeting audio locally."
      }
    },
    "dmg": {
      "title": "StenoAI Installer",
      "contents": [
        {
          "x": 130,
          "y": 220,
          "type": "file"
        },
        {
          "x": 410,
          "y": 220,
          "type": "link",
          "path": "/Applications"
        }
      ],
      "window": {
        "width": 540,
        "height": 380
      },
      "artifactName": "stenoAI-macos-${version}-${arch}.${ext}",
      "sign": true
    },
    "publish": {
      "provider": "github",
      "owner": "akashiitd",
      "repo": "Mac_meeting_Transcriber_app"
    }
  }
}
